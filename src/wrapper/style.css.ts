export const WrapperStyleCSS = `
    html, body, iframe {
        width: 100vw;
        height: 100vh;
    
        border: 0;
        margin: 0;
        padding: 0;
    
        font-family: sans-serif;
    }
    
    .zig-clock {
        position: absolute;
        font-size: 0.7em; 
        padding: 0.5em;
        margin: -0.25em;
        border-radius: 0.25em;  
        z-index: 100;
    }
`;
